# Goida VPN Site - Заметки по проекту

## 🏗 Структура проекта

- **main.py**: Основное приложение Flask.
  - Служит веб-сервером (использует `waitress` для локального хостинга).
  - Маршруты:
    - `/`: Отрисовывает главную страницу.
    - `/api/download-links`: Предоставляет динамические ссылки на скачивание инструментов (v2rayNG, Throne) с кэшированием на 24 часа.
    - `/api/github-stats`: Предоставляет статистику репозитория с кэшированием на 1 час.
    - `/LICENSE`: Отдает файл лицензии.

- **services.py**: Бэкенд-логика для управления конфигурациями.
  - Получает и парсит конфигурации VPN из репозитория `AvenCores/goida-vpn-configs`.
  - Извлекает из `README.md` этого репозитория временные метки последних обновлений.
  - Сопоставляет ID конфигов с их соответствующими URL-адресами источников.

- **build.py**: Скрипт сборки статического сайта и деплоя.
  - Рендерит `index.html` в директорию `dist/`.
  - Генерирует статические JSON-файлы для API (`api/download-links.json`, `api/github-stats.json`), чтобы сайт мог полноценно работать на GitHub Pages.
  - Управляет деплоем в ветку `gh-pages`, используя GitHub Token (`MY_TOKEN`).

- **static/**: Статические ресурсы.
  - **js/**:
    - `statistics.js`: Асинхронно загружает и отображает статистику GitHub.
    - `update-download-links.js`: Периодически обновляет ссылки на скачивание из API.
    - `link-confirmation.js`: Управляет модальными окнами подтверждения выхода и скачивания.
  - **images/**: Фавиконы и другие изображения интерфейса.
  - **media/**: Логотипы партнеров (Dzen, Sber и т.д.).
  - **css/**: Пользовательские стили (хотя в основном используется Tailwind через CDN).

- **templates/**: Шаблоны Jinja2.
  - `base.html`: Основной макет с управлением состоянием через Alpine.js (для модалок и переключения тем).
  - `index.html`: Главная страница, объединяющая различные компоненты.
  - **components/**: Модульные части UI (шапка, подвал, вкладки с конфигами, инструкции и т.д.).

## ⚙️ Технологии
- **Бэкенд**: Python (Flask, Waitress, Requests).
- **Фронтенд**: TailwindCSS (CDN), Alpine.js (CDN), FontAwesome.
- **CI/CD**: Скрипт `build.py` + GitHub Actions (`.github/workflows/deploy.yml`).

## 🔑 Ключевые особенности
- **Динамические конфигурации**: Конфиги подтягиваются из внешнего репозитория, что позволяет обновлять их без пересборки сайта.
- **Статический и динамический режимы**: Работает и как живое Flask-приложение, и как предварительно отрендеренный статический сайт.
- **Адаптивность**: Полностью отзывчивый дизайн, оптимизированный для мобильных устройств.
- **Поддержка тем**: Светлая и темная темы.
- **Безопасность ссылок**: Модальные окна подтверждения для внешних переходов и загрузок.

## 📝 Обслуживание
- Чтобы обновить конфиги: внесите изменения в репозиторий `AvenCores/goida-vpn-configs`.
- Чтобы изменить логику сайта: редактируйте `services.py` или `main.py`.
- Чтобы обновить интерфейс: редактируйте шаблоны в `templates/components/`.
- Для деплоя: запустите `python build.py` (требуется переменная окружения `MY_TOKEN`).